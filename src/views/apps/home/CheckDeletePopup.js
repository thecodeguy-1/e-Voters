import { useState } from 'react'
import { Row, Col, Card, CardBody, Button, Modal, ModalHeader, ModalBody, ModalFooter } from 'reactstrap'
const CheckDeletePopup = () => {
 
    return (
        <>
       
            {/* checkpopup  */}
            {/* deeletepopup */}
            <Modal >
         <ModalHeader >Create an Election</ModalHeader>
         <ModalBody>
             ccc
         </ModalBody>
         <ModalFooter>
             <div style={{display:'flex', justifyContent:'space-between', width:'100%' }}>
      ccc
             </div>
         </ModalFooter>
         </Modal>
             {/* deeletepopup */}
                </>
    )
}

export default CheckDeletePopup